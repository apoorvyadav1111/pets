'use strict';

describe('myApp.sell module', function() {

    beforeEach(module('myApp.sell'));

    describe('sell controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view3Ctrl = $controller('View3Ctrl');
            expect(view3Ctrl).toBeDefined();
        }));

    });
});